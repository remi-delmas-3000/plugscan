# Correction 2026-08-31: the earlier skip ("no stable site or changelog yet")
# is stale. phaseburn.music is live and its homepage embeds JSON-LD with
# "alternateName":"Phaseburn ONE","softwareVersion":"0.1.1" served statically
# (verified live against installed 0.1.1). Note: builds are beta (0.x), so the
# default prerelease exclude must not see the word "beta" - the regex captures
# only the JSON-LD number.
vendor = "Phaseburn Music"
homepage = "https://phaseburn.music"

[manual]
steps = "Download Phaseburn ONE from phaseburn.music; beta build announcements also on the KVR Instruments forum thread."

[[product]]
name = "Phaseburn ONE"
page = "https://phaseburn.music/"
version_regex = '"softwareVersion":"([0-9]+\.[0-9.]+)"'
download = "https://phaseburn.music/"
