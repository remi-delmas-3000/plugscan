# No public version source. Plugin versions live only behind the authenticated
# account/download portal (access.mixedinkey.com/api, login-gated); the public
# marketing pages carry no maintained per-product version, and the portal API
# returns a login SPA (HTTP 404) to unauthenticated clients. Verified live 2026-08.
vendor = "Mixed In Key"
homepage = "https://mixedinkey.com"
download_via = "Mixed In Key account portal (access.mixedinkey.com) — sign in to download the current installer"
skip = "Versions only in the login-gated account portal (access.mixedinkey.com); no public version endpoint or changelog. Each product ships its own installer; re-verified 2026-08-31: the Sparkle appcast at builds.mixedinkey.com needs a per-product key param (404 without it) and no unauthenticated catalog exists"
[manual]
login = "https://access.mixedinkey.com"
steps = "Log in at access.mixedinkey.com (or mixedinkey.com, top-right Login) and download the latest Captain Plugins / Pilot / Human / Wingman installers from your account. Each product's installer carries its own version (Captain 7.x, Pilot 2.x); run it to update. No public changelog with versions."
